# --- Base de datos ---
# Local (por defecto): archivo SQLite dentro de ./data
DATABASE_URL="file:./data/premio.db"
# Produccion con Turso (opcional):
# DATABASE_URL="libsql://tu-base.turso.io"
# DATABASE_AUTH_TOKEN="ey..."

# --- Seguridad del panel ---
# Cambiar SI o SI en produccion (usar una cadena larga y aleatoria)
SESSION_SECRET="cambiar-esta-clave-por-una-larga-y-aleatoria"

# --- Fecha de cierre de la convocatoria (ISO) ---
PUBLIC_FECHA_CIERRE="2026-09-30T23:59:00-03:00"
